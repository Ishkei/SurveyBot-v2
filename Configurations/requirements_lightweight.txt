# Lightweight Vision Bot Requirements
# Based on Blue Parker's vision-based approach without heavy dependencies

# Core dependencies (CAPTCHA dependencies removed)
numpy>=1.24.0
requests>=2.31.0

# AI and personality
google-generativeai>=0.3.0
anthropic>=0.7.0

# Utilities
python-dotenv>=1.0.0
asyncio>=3.4.3

# Enhanced features (optional) (CAPTCHA dependencies removed)
