# Minimal Slider CAPTCHA Solver Requirements
# Only essential packages for basic CAPTCHA solving

# Core image processing (essential)
opencv-python>=4.8.0
pillow>=10.0.0
numpy>=1.24.0

# Mouse control for slider movements (essential)
pynput>=1.7.6

# Web automation (already installed)
selenium>=4.15.0
undetected-chromedriver>=3.5.0

# HTTP requests and environment (essential)
requests>=2.31.0
python-dotenv>=1.0.0
